spring:
    datasource:
        url: jdbc:h2:file/data/fiscaliza_db
        username: sa
        password: password
        name: fiscaliza_db
        driver-class-name: org.h2.Driver
    jpa:
        database-platform: org.hibernate.dialect.h2
        show-sql: true
        hibernate:
            ddl-auto: update